version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: isalvama-mysql
    restart: unless-stopped
    environment:
      MYSQL_USER: isalvama
      MYSQL_PASSWORD: password123
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
